# V84 - 对局编辑弹窗与对局记录收起

本版本基于 V83 修复版继续修改：

1. 对局详情页中，对局设置不再直接展开显示。
   - 管理员点击“对局编辑”后弹窗编辑对局名称、场地、小队数、小队人数上限。

2. 对局管理首页只显示开局整备中和进行中的对局。
   - 已终局对局不再直接显示在对局管理首页。

3. 新增“对局记录”页面。
   - 普通用户只显示自己参加过的已终局对局。
   - 管理员默认可查看所有已终局对局。
   - 管理员可以切换为“只看我参加过的”。

部署时继续保留：

```text
data/app.db
static/uploads/
```
