# NBYS Training Target Android

Android 靶机应用：会员登录、Camera2 预览、设备心跳、房间绑定、四点透视校准、红色激光识别及命中上传。

自动定位仅匹配 NBYS 自有的黑白同心方形标记。可打印 A4/A3 靶纸位于：

`../design/training-room/targets/`

打印时使用实际大小，并确保四角标记完整进入最终缩放画面。自动定位失败时可继续使用手动四点定位。
